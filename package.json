{
  "name": "pres-solo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Single-scroll explainer site for the Solo + Claude Code + Mulch multi-agent workflow.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.40.0",
    "@tailwindcss/vite": "^4.1.11",
    "tailwindcss": "^4.1.11",
    "vite": "^6.3.5",
    "wrangler": "^4.98.0"
  }
}
